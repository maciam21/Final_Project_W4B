const suits = [ 'd', 'h', 'c','s'];
const values - 

const colors = ['red','black'];

const deck = [];

for let i = 0; i <52; i++) {

    const suit = 4 i % 4;
    const card = {suit : suits
    console.log (suit);
}
